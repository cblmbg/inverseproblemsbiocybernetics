\documentclass[11pt,a4paper]{article}

\usepackage[margin=2.4cm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,calc,positioning,shadows.blur}
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{xcolor}
\usepackage{microtype}
\usepackage{caption}
\usepackage{float}
\usepackage{listings}
\usepackage[hidelinks]{hyperref}

\definecolor{codeblue}{RGB}{32,80,130}
\definecolor{codegray}{RGB}{245,246,248}
\definecolor{coderule}{RGB}{210,214,220}

\lstdefinestyle{matlabstyle}{
  language=Matlab,
  basicstyle=\ttfamily\small,
  keywordstyle=\color{codeblue}\bfseries,
  backgroundcolor=\color{codegray},
  frame=single,
  rulecolor=\color{coderule},
  breaklines=true,
  showstringspaces=false,
  columns=fullflexible
}

\lstdefinestyle{pseudostyle}{
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\color{codeblue}\bfseries,
  backgroundcolor=\color{codegray},
  frame=single,
  rulecolor=\color{coderule},
  breaklines=true,
  showstringspaces=false,
  columns=fullflexible,
  keepspaces=true,
  morekeywords={Input,Output,for,each,end,repeat,until,solve,select,return}
}

\newcommand{\vect}[1]{\boldsymbol{#1}}
\newcommand{\R}{\mathbb{R}}

\title{\textbf{A Four-Level Inverse-Problem Case Study}\\
\large MATLAB implementation for a synthetic microbial consortium}
\author{Technical implementation report accompanying the manuscript\\
\emph{A Hierarchical Framework for Inverse Problems in Biological Cybernetics}}
\date{23 July 2026}

\begin{document}

\maketitle

\begin{abstract}
This report presents a reproducible MATLAB case study that turns the inverse
ladder into a single biological example. We consider two microbial strains that
compete for a supplied substrate while exchanging metabolites, and ask four
progressively deeper questions: which kinetic parameters govern their dynamics,
which growth law can be discovered from observations, whether their behavior
can be interpreted as optimizing a community-level objective, and whether it
is better explained as a strategic interaction between the two strains.
Synthetic data with known ground truth make it possible to assess each level
while keeping the biological system fixed.

The results show that parameter calibration and sparse model discovery can
reconstruct the observed dynamics, while also exposing structural ambiguities
that a good predictive fit alone cannot resolve. At the higher levels, inverse
optimal control and inverse differential games identify objectives that
reproduce, respectively, centralized and strain-specific allocation behavior
within the simulated setting. The case study therefore illustrates the central
message of the inverse ladder: moving upward changes the goal from estimating
numerical coefficients to inferring mechanisms and behavioral explanations,
but it also requires stronger assumptions and introduces new forms of
non-uniqueness. The implementation is intended as a transparent proof of
concept and as a basis for studying identifiability, experimental design, and
robustness before applying these ideas to real microbial communities.
\end{abstract}

\tableofcontents
\clearpage

\section{Purpose and scope}

The accompanying manuscript \cite{BangaVillaverde2026} proposes a hierarchy in
which each inverse problem inherits the uncertainties of the levels below it
and introduces a qualitatively new source of non-uniqueness. A useful case
study should therefore do more than present four unrelated algorithms. It
should hold the biological setting fixed while changing what is assumed known
and what is inferred.

The present implementation was designed around five requirements:
\begin{enumerate}
  \item use the same microbial system at all four levels;
  \item keep the dynamics small enough for transparent mathematical analysis;
  \item introduce explicit, biologically interpretable control variables for
        Levels 3 and 4;
  \item provide known ground truth through synthetic data; and
  \item expose non-identifiability rather than forcing exact recovery.
\end{enumerate}

The result is a proof-of-concept, not a claim that objectives can already be
reliably inferred from ordinary microbial abundance data. In particular,
dynamic allocation measurements or appropriate molecular proxies would be
needed before applying the upper levels to a real consortium.

Figure~\ref{fig:inverse-ladder-overview} summarizes how the same microbial
plant supports four progressively more explanatory inverse questions, while
also making explicit the change in the epistemic status of the allocation
variables between the lower and upper levels.

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
  \input{figures/inverse_ladder_overview.tikz.tex}%
}
\caption{The four-level inverse ladder for the two-strain cross-feeding case
study. Levels 1 and 2 use known, externally actuated allocations to identify
parameters and dynamic structure. Levels 3 and 4 instead treat observed
allocations as endogenous behavior to be explained by, respectively, a
community objective or strain-specific objectives. The biological plant and
state equations remain fixed, but explanatory depth, assumptions, and the
kind of non-uniqueness increase up the ladder.}
\label{fig:inverse-ladder-overview}
\end{figure}

\section{Biological system and dynamic model}

\subsection{Two-strain cross-feeding consortium}

Two microbial strains \(X_1\) and \(X_2\) compete for a common substrate \(S\).
Strain 1 secretes metabolite \(M_1\), which supports growth of strain 2, while
strain 2 secretes metabolite \(M_2\), which supports growth of strain 1. The
state is
\begin{equation}
  \vect{x}(t)=
  \begin{bmatrix}
    X_1(t) & X_2(t) & S(t) & M_1(t) & M_2(t)
  \end{bmatrix}^{\mathsf T}.
\end{equation}

The dimensionless variable \(u_i(t)\in[0,1]\) represents the effective
fraction of strain \(i\)'s metabolic resources allocated to secretion.
Increasing \(u_i\) increases secretion of \(M_i\), but reduces the strain's
instantaneous growth capacity. This construction creates a biologically
interpretable trade-off between individual growth and support of the partner.

For Levels 1 and 2, the primary laboratory interpretation is an engineered
consortium in which expression of each secretion pathway is actuated by an
external input, such as an inducer concentration or an optogenetic light
signal. Strictly speaking, the experimenter prescribes this actuator rather
than directly setting an intracellular resource fraction. The model assumes
that a previously calibrated actuator-to-allocation relation maps the applied
signal to a known effective input \(u_i(t)\). Thus, ``prescribed allocation''
is shorthand for a prescribed actuator with a known effective allocation; it
does not imply perfect direct control of cellular metabolism.

A different experiment could estimate \(u_i(t)\) from a transcriptional
reporter, flux proxy, or other molecular measurement without externally
actuating the pathway. In that case \(u_i(t)\) would be a measured covariate,
not an externally prescribed input, and its measurement error and possible
dependence on the cellular state should be included in the inverse problem.
That measured-only design is not simulated here.

Throughout the case study, time is measured in hours (h), biomass
concentrations \(X_i\) in grams of dry weight per litre (gDW\,L\(^{-1}\)),
and extracellular substrate and metabolite concentrations \(S\) and \(M_i\)
in millimoles per litre (mmol\,L\(^{-1}\)). The allocation variables \(u_i\)
are dimensionless.

The epistemic status of \(u_i\) changes deliberately along the inverse
ladder. In Levels 1 and 2 it is a known exogenous input used to excite and
identify the dynamics. In Level 3 it becomes an observed endogenous
trajectory hypothesized to solve a centralized optimal-control problem. In
Level 4, \(u_i\) is instead the endogenous strategy of strain \(i\) in a
differential game. The four levels therefore share the same biological
plant and state equations, but the upper levels use separately generated
control trajectories and ask a different question about their origin.
Applying inverse optimal control or an inverse game directly to an externally
imposed \(u_i(t)\) could at most explain the experimenter's actuation policy;
it would not identify a biological objective of the strains.

\subsection{Growth and material-balance equations}

For strain \(i\), let \(j=3-i\) denote its partner. The unburdened specific
growth rate is
\begin{equation}
  \bar{\mu}_i(\vect{x}) =
  \mu_{i,\max}
  \frac{S}{K_{S,i}+S}
  \frac{M_j}{K_{M,i}+M_j},
\end{equation}
and the allocation-dependent growth rate is
\begin{equation}
  \mu_i(\vect{x},u_i)=
  \bar{\mu}_i(\vect{x})(1-c_i u_i).
\end{equation}
The implementation enforces a small positive lower bound on the allocation
factor for numerical robustness. The complete dynamics are
\begin{align}
  \dot X_i &=
  \left(\mu_i(\vect{x},u_i)-D\right)X_i, \label{eq:biomass}\\
  \dot S &=
  D(S_{\mathrm{in}}-S)
  -\sum_{i=1}^{2}\frac{\mu_i(\vect{x},u_i)X_i}{Y_{S,i}},
  \label{eq:substrate}\\
  \dot M_1 &=
  \alpha_1u_1X_1
  -\frac{\mu_2(\vect{x},u_2)X_2}{Y_{M,2}}
  -DM_1,\\
  \dot M_2 &=
  \alpha_2u_2X_2
  -\frac{\mu_1(\vect{x},u_1)X_1}{Y_{M,1}}
  -DM_2. \label{eq:metabolites}
\end{align}

Table~\ref{tab:parameters} lists the ground-truth parameters used to generate
the synthetic data.

\begin{table}[H]
\centering
\caption{Ground-truth parameters of the synthetic microbial consortium.}
\label{tab:parameters}
\begin{tabular}{lccc}
\toprule
Quantity & Strain 1 & Strain 2 & Unit\\
\midrule
Maximum growth rate, \(\mu_{i,\max}\) & 0.55 & 0.50 & h\(^{-1}\)\\
Substrate half-saturation, \(K_{S,i}\) & 0.80 & 1.00 & mmol\,L\(^{-1}\)\\
Metabolite half-saturation, \(K_{M,i}\) & 0.12 & 0.10 & mmol\,L\(^{-1}\)\\
Allocation cost, \(c_i\) & 0.35 & 0.30 & dimensionless\\
Secretion rate, \(\alpha_i\) & 0.55 & 0.50 &
  mmol\,gDW\(^{-1}\)\,h\(^{-1}\)\\
Substrate yield, \(Y_{S,i}\) & 0.65 & 0.60 & gDW\,mmol\(^{-1}\)\\
Metabolite yield, \(Y_{M,i}\) & 8.0 & 8.0 & gDW\,mmol\(^{-1}\)\\
\midrule
Dilution rate, \(D\) & \multicolumn{2}{c}{0.05} & h\(^{-1}\)\\
Inlet substrate, \(S_{\mathrm{in}}\) & \multicolumn{2}{c}{10.0} &
  mmol\,L\(^{-1}\)\\
\bottomrule
\end{tabular}
\end{table}

\section{Synthetic experiments and numerical implementation}

\subsection{Information content, exogeneity, and identifiability}

External actuation of \(u_i\) is a useful experimental-design choice for the
lower levels, but it is neither a logical requirement nor a guarantee of
identifiability. What is essential is that the observed trajectories contain
enough independent information about the unknown quantities. For Level 1,
local practical identifiability is related to the rank and conditioning of
the stacked parameter-sensitivity matrix
\begin{equation}
  \mathcal{S}_{\theta} =
  \begin{bmatrix}
    \partial\vect{x}(t_1)/\partial\vect{\theta}\\
    \vdots\\
    \partial\vect{x}(t_N)/\partial\vect{\theta}
  \end{bmatrix}.
\end{equation}
If its columns are independent over the collection of experiments, parameter
effects can in principle be separated locally; poor conditioning still makes
their estimates sensitive to finite sampling and noise. For Level 2, the
analogous requirement is that the columns of the regression library be
sufficiently distinct over the sampled state--allocation region. In
particular, the data should not make \(q_i\), \(u_i\), and \(q_i u_i\) nearly
collinear.

Rich endogenous allocations can satisfy these information conditions. Direct
actuation of \(u_i\) is therefore not indispensable if its natural variation
is measured accurately and the community visits a sufficiently broad set of
states. However, endogeneity introduces an additional identification issue.
If allocation is generated by feedback,
\begin{equation}
  u_i(t)=\pi_i\bigl(\vect{x}(t)\bigr),
\end{equation}
the observations explore the closed-loop trajectories produced jointly by
the biological dynamics and the policy \(\pi_i\). Different combinations of
plant dynamics and feedback can then agree on the observed trajectories.
Moreover, process disturbances or unmeasured physiological variables may
correlate \(u_i\) with the equation error, so treating it as an ordinary
error-free input can bias parameter estimates or favor an incorrect model
term. Such data require a closed-loop identification treatment, a joint model
of the allocation mechanism, suitable instrumental information, or an
explicit measurement-error model.

External actuation helps by breaking some state--allocation correlations,
expanding the explored operating region, and improving the conditioning of
the sensitivity and library matrices. It cannot rescue an intrinsically
non-identifiable model, and a constant or poorly designed actuator remains
uninformative. Conversely, \(u_i\) may be left endogenous while initial
conditions, inlet substrate \(S_{\mathrm{in}}\), dilution rate \(D\), or other
environmental conditions are varied across experiments. The resulting family
of natural responses may also provide adequate excitation.

Thus, structural identifiability concerns uniqueness permitted by the model,
observation scheme, and admissible inputs, whereas practical identifiability
concerns the information and noise level in the realized dataset. The present
case study uses exogenous allocation actuation to make the Level 1 and Level 2
demonstrations controlled and well conditioned, not because endogenous data
are fundamentally unusable.

\subsection{Calibration experiments}

Three calibration experiments use different initial biomass ratios, substrate
levels, and metabolite concentrations. For the synthetic engineered-consortium
interpretation, the externally applied actuator signals are converted through
the assumed calibrated input map into the effective allocations \(u_1(t)\)
and \(u_2(t)\). These known inputs are persistently exciting square-wave
schedules with small sinusoidal components and are imposed independently of
the observed state trajectories. Across the three experiments, the realized
ranges are \(u_1\in[0.130,0.730]\) and \(u_2\in[0.150,0.730]\). The safety
clamp \([0.08,0.82]\) in the data generator is never active. This realized
variation is important because \(u_i\) affects both secretion through
\(\alpha_i u_iX_i\) and the growth burden through \(c_i u_i\); varying it
helps separate the secretion coefficients \(\alpha_i\) from the allocation
costs \(c_i\). States are sampled at 49 times over 24 h and corrupted
with independent Gaussian noise at \(0.5\%\) of the maximum state scale.

\subsection{Upper-level demonstration experiments}

Levels 3 and 4 use a separate, smaller collection of synthetic behavioral
demonstrations rather than the three calibration experiments. Each upper
level uses two initial states. For both initial states, the control horizon is
\(T=16\) h with grid
\[
  t_k=0,2,\ldots,16\ \text{h},
\]
giving eight piecewise-constant control intervals of 2 h each. Level 3
therefore generates two centralized-planner demonstrations, whereas Level 4
generates two open-loop Nash demonstrations. The same horizon and initial
states are used for the corresponding forward validations. Keeping these
upper-level data separate from the calibration data is necessary because
their allocations are endogenous behaviors generated by the hypothesized
planner or game, rather than externally imposed identification inputs.

\subsection{Two simulation modes}

The MATLAB implementation contains two complementary integrators that solve
the same biological ODEs:
\begin{itemize}
  \item \texttt{simulateCommunity.m} uses \texttt{ode15s} with interpolated
        controls and non-negativity constraints;
  \item \texttt{simulateControlledModel.m} uses fixed-step fourth-order
        Runge--Kutta integration for the repeated simulations required by
        optimization.
\end{itemize}

The adaptive \texttt{ode15s} solver is used as the reference integrator for
generating or validating experimental trajectories. Its variable step size
and error control are appropriate when accuracy and robustness to possible
stiffness are more important than a fixed computational cost. It also supports
continuous interpolation of measured control profiles and direct enforcement
of state non-negativity.

The fixed-step Runge--Kutta solver is used inside the parameter-estimation,
optimal-control, finite-difference, and best-response loops. These algorithms
may evaluate the model thousands of times. A prescribed integration grid gives
each evaluation the same number of right-hand-side calls, aligns exactly with
the piecewise-constant control parameterization, and avoids small changes in
adaptive step selection appearing as numerical irregularities in objective
functions or finite-difference gradients. For this low-dimensional model, it
is therefore substantially faster and more reproducible within optimization.

The two modes are not different models and are not expected to produce
different biological conclusions. The fixed RK4 step was chosen only after a
convergence check against \texttt{ode15s}; the unit tests require their
all-state RMS difference to remain below \(2\times10^{-3}\) on a representative
experiment. If the model were made substantially stiffer, this comparison
would need to be repeated and the optimization integrator or step size revised.

\subsection{Software entry point}

The entire workflow is launched by
\begin{lstlisting}[style=matlabstyle]
results = runInverseLadderCaseStudy;
\end{lstlisting}
The command generates data, executes the four levels, performs forward
validation, saves \texttt{inverse\_ladder\_results.mat}, and exports the
summary graphics.

\section{Level 1: parameter estimation}

\subsection{Inverse problem}

The model structure in equations~\eqref{eq:biomass}--\eqref{eq:metabolites}
is assumed known. The effective allocation schedules
\(\vect{u}_e(t)=[u_{1,e}(t),u_{2,e}(t)]^{\mathsf T}\) are also known for each
experiment \(e\). Six quantities are estimated:
\begin{equation}
  \vect{\theta} =
  [\mu_{1,\max},\mu_{2,\max},\alpha_1,\alpha_2,c_1,c_2]^{\mathsf T}.
\end{equation}
All remaining parameters are treated as independently characterized. The
weighted least-squares problem is
\begin{equation}
  \hat{\vect{\theta}} =
  \arg\min_{\vect{\theta}_{\min}\le\vect{\theta}\le\vect{\theta}_{\max}}
  \sum_{e,k,\ell}
  \left[
    \frac{x^{\mathrm{sim}}_{e,k,\ell}(\vect{\theta};\vect{u}_e)
          -x^{\mathrm{obs}}_{e,k,\ell}}
         {s_{\ell}}
  \right]^2,
\end{equation}
where \(e\), \(k\), and \(\ell\) index experiments, times, and states,
respectively, and \(s_{\ell}\) is a state-specific scale.

This is a parameter-estimation problem conditional on the applied inputs.
Every candidate parameter vector is simulated under exactly the same
\(\vect{u}_e(t)\). The least-squares optimizer neither estimates these
schedules nor changes them to improve the fit, and Level 1 makes no claim
that they are optimal choices made by the microorganisms. This distinction
is what is meant by ``externally prescribed'' in Figure~\ref{fig:level1}.

The problem is solved with \texttt{lsqnonlin} from two truth-independent
initial points: the midpoint of the parameter bounds and a seed-controlled
uniform random draw within those bounds. Thus, the synthetic ground-truth
vector is not used to initialize the optimizer; it is accessed only after
estimation to quantify recovery error. Bound constraints enforce biologically
meaningful values.

\noindent\begin{minipage}{\linewidth}
\begin{lstlisting}[style=pseudostyle,title={Compact MATLAB pseudocode}]
Input: experiments {t_e, xobs_e, u_e}, parameter bounds, initial guesses
for each initial guess theta0
    theta <- lsqnonlin(residual, theta0, bounds)
    residual(theta):
        for each experiment e
            xsim_e <- simulateControlledModel(theta, x0_e, t_e, u_e)
            append scaled(xsim_e - xobs_e)
select theta_hat with the smallest residual norm
Output: theta_hat and fitted state trajectories
\end{lstlisting}
\end{minipage}

\subsection{Results}

\begin{table}[H]
\centering
\caption{Level 1 parameter-estimation results.}
\label{tab:level1}
\begin{tabular}{lrrr}
\toprule
Parameter & True & Estimated & Relative error (\%)\\
\midrule
\(\mu_{1,\max}\) & 0.55000 & 0.55080 &  0.145\\
\(\mu_{2,\max}\) & 0.50000 & 0.49919 & -0.162\\
\(\alpha_1\)     & 0.55000 & 0.54964 & -0.065\\
\(\alpha_2\)     & 0.50000 & 0.50039 &  0.078\\
\(c_1\)          & 0.35000 & 0.35175 &  0.501\\
\(c_2\)          & 0.30000 & 0.29614 & -1.287\\
\bottomrule
\end{tabular}
\end{table}

The RMS relative error across all six parameters is \(0.572\%\).
Figure~\ref{fig:level1} shows the observed and fitted trajectories for the
first calibration experiment. The fitted model reproduces biomass, substrate,
and metabolite dynamics while being driven by the prescribed effective
allocation
schedules.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/level1_calibration.png}
\caption{Level 1 calibration for the first synthetic experiment. Circles are
noisy observations; solid curves are predictions obtained with the estimated
parameters. The bottom-right panel shows the externally prescribed allocation
inputs \(u_i(t)\), interpreted as effective allocations produced by calibrated
external actuation of the secretion pathways.}
\label{fig:level1}
\end{figure}

\section{Level 2: model discovery}

\subsection{Candidate library}

The environmental balances are retained, while the per-capita biomass growth
law is treated as unknown. As in Level 1, the allocation schedules are known
exogenous inputs rather than quantities discovered by the regression. This
library-based sparse-regression construction follows the general
data-driven model-discovery paradigm \cite{BruntonKutz2022}. For each strain,
the candidate library is constructed from:
\begin{equation}
  \vect{\phi} =
  \begin{bmatrix}
  q_i &
  q_i u_i &
  X_1 &
  X_2 &
  S &
  M_j &
  u_i
  \end{bmatrix},
  \qquad
  q_i =
  \frac{S}{K_{S,i}+S}
  \frac{M_j}{K_{M,i}+M_j}.
\end{equation}

Rather than differentiating noisy biomass measurements directly, the method
uses window-integrated per-capita growth:
\begin{equation}
  \frac{\log X_i(t_b)-\log X_i(t_a)}{t_b-t_a}
  \approx
  \beta_{i,0}
  + \frac{1}{t_b-t_a}
    \int_{t_a}^{t_b}\vect{\phi}(t)^{\mathsf T}\vect{\beta}_i\,dt.
\end{equation}
Savitzky--Golay smoothing is applied before integration. LASSO selects a sparse
coefficient vector using cross-validation.

This construction is closely related to weak SINDy
\cite{MessengerBortz2021}. With \(z_i=\log X_i\), the window equation is the
integral, or constant-test-function, form of
\(\dot z_i=\beta_{i,0}+\vect{\phi}^{\mathsf T}\vect{\beta}_i\): it replaces a
pointwise derivative by endpoint differences and integrals of the candidate
library. General WSINDy instead uses a bank of smooth, compactly supported test
functions and transfers derivatives to those functions by integration by
parts. The present algorithm is therefore best viewed as a restricted
weak-form SINDy method using boxcar windows, explicit smoothing,
cross-validated LASSO, and post-fit thresholding.

\noindent\begin{minipage}{\linewidth}
\begin{lstlisting}[style=pseudostyle,title={Compact MATLAB pseudocode}]
Input: calibration experiments and Level 1 parameter estimates
for each strain i
    smooth the measured state trajectories
    for each admissible time window
        compute window-averaged per-capita growth y
        compute averaged library row [q_i, q_i*u_i, X1, X2, S, Mj, u_i]
    betaPath, CV <- lasso(Phi, y)
    beta_i <- coefficients at minimum cross-validation error
    threshold small coefficients and compute R2
Output: sparse support and coefficients for both growth laws
\end{lstlisting}
\end{minipage}

\subsection{Results and structural ambiguity}

\begin{table}[H]
\centering
\caption{Level 2 sparse growth-law coefficients. Zeros denote terms removed
by regularization.}
\label{tab:level2}
\begin{tabular}{lrr}
\toprule
Candidate term & Strain 1 & Strain 2\\
\midrule
Intercept & -0.0529 & -0.0252\\
Monod cross-feeding, \(q_i\) & 0.5200 & 0.4157\\
Allocation interaction, \(q_i u_i\) & -0.2090 & 0\\
Strain 1 abundance & 0 & 0\\
Strain 2 abundance & 0 & 0\\
Substrate & 0 & 0\\
Received metabolite & 0 & 0\\
Direct allocation, \(u_i\) & 0 & -0.0496\\
\midrule
\(R^2\) & 0.9497 & 0.9458\\
\bottomrule
\end{tabular}
\end{table}

For strain 1, the discovered support agrees with the generating model:
growth depends on \(q_1\) and the interaction \(q_1u_1\). For strain 2, the
method selects a direct allocation term instead of \(q_2u_2\), despite
achieving \(R^2=0.946\). This is not treated as a software failure. It is an
example of structural indistinguishability caused by correlated candidate
functions over the observed region of state-control space. A high predictive
score does not guarantee recovery of the correct biological mechanism.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/inverse_ladder_summary.png}
\caption{Summary of the four inverse levels. The Level 2 heat map displays the
selected sparse coefficients, while the other panels compare ground-truth and
estimated parameters or objective weights.}
\label{fig:summary}
\end{figure}

\section{Level 3: inverse optimal control}

\subsection{Centralized-planner hypothesis}

In contrast to Levels 1 and 2, the allocation trajectories are now endogenous:
they are observations to be explained rather than actuated identification
inputs. At Level 3, the consortium is provisionally treated as a single
decision maker choosing both allocation trajectories. This is a hypothesis to
be tested, not an assertion of community-level selection. Multilevel
optimization has previously been used for forward metabolic modeling of
microbial communities \cite{Zomorrodi2012}; here the objective is instead
inferred from dynamic trajectories. The finite-horizon objective is linear in
four normalized kernels:
\begin{equation}
  J_C(\vect{u};\vect{w}) =
  \sum_{m=1}^{4}w_m F_m(\vect{x},\vect{u}),
  \qquad
  \vect{w}\ge 0,\qquad
  \vect{1}^{\mathsf T}\vect{w}=1.
\end{equation}
The kernels represent:
\begin{enumerate}
  \item negative total biomass, so minimization favors biomass production;
  \item quadratic allocation effort;
  \item residual substrate;
  \item squared biomass imbalance \((X_1-X_2)^2\).
\end{enumerate}

The forward optimal-control problem uses piecewise-constant controls on eight
intervals and fourth-order Runge--Kutta integration. It is solved with
\texttt{fmincon} and box constraints \(0.02\le u_i\le0.95\).

\subsection{Relation to bilevel inverse optimal control}

The general inverse optimal-control framework of Tsiantis, Balsa-Canto, and
Banga \cite{Tsiantis2018} was formulated as a bilevel problem. In a simplified
version with observed allocation controls and known dynamics, the structure
would be
\begin{equation}
  \hat{\vect{w}}
  =
  \arg\min_{\vect{w}\in\Delta}
  D\!\left(\vect{u}^{\mathrm{obs}},\vect{u}^{*}(\vect{w})\right),
  \qquad
  \vect{u}^{*}(\vect{w})
  \in
  \arg\min_{\vect{u}\in\mathcal{U}}
  J_C(\vect{u};\vect{w}),
  \label{eq:bilevel-ioc}
\end{equation}
where \(\Delta=\{\vect{w}\ge0:\vect{1}^{\mathsf T}\vect{w}=1\}\),
\(\mathcal{U}\) includes the dynamic and control constraints, and \(D\) is a
trajectory-mismatch metric. More general versions can estimate model
parameters and unmeasured controls simultaneously. A direct numerical
solution of~\eqref{eq:bilevel-ioc} would call the forward optimal-control
solver inside every outer evaluation of a candidate \(\vect{w}\).

Because that nested problem is demanding, the practical strategy in
\cite{Tsiantis2018} decomposed it into input and parameter reconstruction,
generation of a multiobjective Pareto set of optimal controls, and selection
of the Pareto solution most compatible with the data. The MATLAB case study
implemented here uses neither a direct bilevel solve nor that Pareto-set
decomposition. It assumes that the kinetic model has already been calibrated
and that the endogenous allocation trajectories are observed. It then replaces
the inner optimization problem by first-order stationarity conditions at those
observed trajectories.

\subsection{Inverse stationarity formulation}

Because the objective is linear in \(\vect{w}\), its control gradient at an
observed optimum \(\vect{u}^{*}\) can be written
\begin{equation}
  \nabla_{\vect{u}}J_C(\vect{u}^{*};\vect{w})
  =
  \begin{bmatrix}
    \nabla_{\vect{u}}F_1 &
    \cdots &
    \nabla_{\vect{u}}F_4
  \end{bmatrix}
  \vect{w}
  = A_C\vect{w}.
\end{equation}
Finite differences provide the feature gradients. Rows associated with active
control bounds are excluded when enough interior controls are available. The
inverse problem is
\begin{equation}
  \hat{\vect{w}} =
  \arg\min_{\vect{w}\ge0,\ \vect{1}^{\mathsf T}\vect{w}=1}
  \|A_C\vect{w}\|_2^2
  +\lambda\|\vect{w}-\vect{w}_0\|_2^2,
\end{equation}
with a very small regularization parameter
\(\lambda=10^{-8}\) and uniform reference \(\vect{w}_0\).

The singular values of \(A_C\) provide a diagnostic of how much information
the observed controls contain about the objective weights. If
\(A_C=U\Sigma V^{\mathsf T}\), each right singular vector in \(V\) represents
an orthogonal combination of objective weights, and its corresponding singular
value measures how strongly that combination affects the stationarity
residual. A large singular value denotes a weight direction that is readily
distinguished because it produces a large violation of stationarity. A
near-zero singular value instead identifies a combination of weights that is
compatible with the observed optimum. Because multiplying an objective by a
positive constant does not change its optimizer, the simplex constraint
\(\vect{1}^{\mathsf T}\vect{w}=1\) removes this unavoidable scale ambiguity.
After normalization, one isolated small singular value supports a locally
well-defined objective direction, whereas several comparably small singular
values would indicate that multiple objectives are practically
indistinguishable from the available demonstrations. In numerical data, an
exact zero is generally replaced by a small value because of finite-difference
error, discretization, and noise.

\noindent\begin{minipage}{\linewidth}
\begin{lstlisting}[style=pseudostyle,title={Compact MATLAB pseudocode}]
Input: model, initial states, candidate community features
for each of 2 synthetic experiments e
    demo_e <- solveCommunityPlanner(model, x0_e, trueWeights)
    A_e <- finiteDifferenceFeatureJacobian(demo_e, communityCostFeatures)
    retain stationarity rows for interior controls and stack into A_C
w_hat <- inferSimplexWeights(A_C, lambda)
for each experiment e
    validation_e <- solveCommunityPlanner(model, x0_e, w_hat,
                                           initial controls = 0.35)
Output: inferred weights and control/state validation errors
\end{lstlisting}
\end{minipage}

Thus, \texttt{solveCommunityPlanner} is used to create the synthetic
demonstrations and, after inference, to perform forward validation. It is not
nested inside \texttt{inferSimplexWeights}; the inverse calculation itself is
a constrained linear least-squares problem solved with \texttt{lsqlin}. This
is substantially cheaper than bilevel optimization, but it establishes only
compatibility with local first-order conditions. Stationarity alone does not
exclude a saddle point, a local maximum, or a non-global local minimum, and it
does not jointly reconstruct unknown controls or kinetic parameters. Forward
re-optimization is therefore an essential validation step, but does not turn
the inverse calculation into a bilevel method. To make this check independent
of the demonstrated solution, every validation solve is cold-started from the
uniform default \(u_1=u_2=0.35\) on all control intervals. The observed
controls are deliberately not supplied as the initial guess: doing so would
start \texttt{fmincon} essentially at the answer and could produce zero
trajectory error after a single iteration, which is only a warm-start
consistency check.

\subsection{Results}

\begin{table}[H]
\centering
\caption{Level 3 centralized objective weights.}
\label{tab:level3}
\begin{tabular}{lrr}
\toprule
Objective kernel & True & Inferred\\
\midrule
Total biomass & 0.6200 & 0.6098\\
Control effort & 0.1400 & 0.1391\\
Residual substrate & 0.1400 & 0.1519\\
Community imbalance & 0.1000 & 0.0991\\
\bottomrule
\end{tabular}
\end{table}

The Euclidean weight error is \(1.57\times10^{-2}\). Both cold-started forward
solves converged with a positive \texttt{fmincon} exit flag and closely
reproduced the demonstrations, but not identically; Table~\ref{tab:level3-validation}
reports the resulting nonzero discrepancies.

\begin{table}[H]
\centering
\caption{Level 3 cold-start forward validation. RMSE values compare the
re-optimized trajectories obtained with the inferred weights against the
synthetic demonstrations.}
\label{tab:level3-validation}
\begin{tabular}{lrrr}
\toprule
Experiment & Iterations & Control RMSE & State RMSE\\
\midrule
1 & 46 & \(4.491\times10^{-5}\) & \(5.182\times10^{-5}\)\\
2 & 44 & \(1.326\times10^{-5}\) & \(1.955\times10^{-5}\)\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.55\textwidth]{figures/level3_ioc.png}
\caption{Level 3 results for the first experiment. Starting from the uniform
default allocation \(u_1=u_2=0.35\), the forward solve with the inferred
objective closely reproduces the observed optimal controls and biomass
trajectories. The bottom-right panel shows the singular-value spectrum of the
inverse stationarity matrix.}
\label{fig:level3}
\end{figure}

The singular values of the stacked stationarity matrix are approximately
\[
  8.23\times10^{-1},\quad
  2.31\times10^{-1},\quad
  2.2\times10^{-3},\quad
  \text{near zero}.
\]
Here the final, near-zero singular value corresponds to the objective-weight
direction that best satisfies the observed stationarity conditions. The next
smallest singular value represents the closest competing direction. Their ratio
is approximately 641, so the preferred direction is well separated from that
alternative and the normalized objective is locally well defined for these
synthetic demonstrations. This is a local identifiability diagnostic, not proof
that the same objective would remain identifiable under a different feature
library, noisier measurements, or less informative trajectories.

\section{Level 4: inverse differential game}

\subsection{Player-specific objectives}

The centralized hypothesis is replaced by two independent decision makers,
following the inverse noncooperative dynamic-game perspective
\cite{Molloy2022}. Here each \(u_i(t)\) is interpreted as strain \(i\)'s
endogenous strategy, rather than as an externally actuated input. Player \(i\)
minimizes
\begin{equation}
  J_i(u_i,u_j;\vect{w}_i)
  =
  \sum_{m=1}^{4}w_{i,m}F_{i,m},
  \qquad
  \vect{w}_i\ge0,\qquad
  \vect{1}^{\mathsf T}\vect{w}_i=1.
\end{equation}
The four kernels are:
\begin{enumerate}
  \item negative own biomass;
  \item regularized own quadratic allocation effort;
  \item saturating deficit of the metabolite received from the partner;
  \item negative partner biomass.
\end{enumerate}
More precisely, let
\(\overline{g}=T^{-1}\int_0^T g(t)\,\mathrm{d}t\), let \(M_i\) denote the
metabolite produced by player \(i\), and let \(M_j\) denote the metabolite
that it receives from its partner. The implemented features are
\begin{align}
  F_{i,1} &= -\overline{X_i}/B_0,
  &
  F_{i,2} &= \overline{u_i^2}
             +\epsilon_M\overline{M_i^2},\\
  F_{i,3} &= \overline{\frac{K_{\mathrm{def}}}
                                  {K_{\mathrm{def}}+M_j}},
  &
  F_{i,4} &= -\overline{X_j}/B_0,
\end{align}
with \(B_0=2\), \(K_{\mathrm{def}}=0.35\) mmol\,L\(^{-1}\), and
\(\epsilon_M=10^{-5}\) in the numerical concentration units used by the
implementation. Thus, \(F_{i,3}\) approaches one when the received metabolite
is absent and decreases toward zero as it becomes abundant.

The \(\epsilon_M\overline{M_i^2}\) contribution in \(F_{i,2}\) is a numerical
regularizer rather than a proposed biological expenditure. Because the
produced metabolite depends on \(u_i\), it adds weak curvature and removes
otherwise flat directions when that metabolite is abundant. Across the four
player--experiment demonstrations, it contributes only
\(0.00013\%\)--\(0.00083\%\) of \(F_{i,2}\), so it does not materially change
the objective values. Nevertheless, it is part of the feature basis and can
affect the stationarity-matrix conditioning. The singular-value diagnostics
reported below are therefore conditional on this explicitly regularized
basis, consistent with the broader objective-basis-dependence caveat.

The final term permits a general-sum interaction: supporting the partner can
be beneficial because the partner supplies an essential metabolite.

An open-loop Nash equilibrium
\((u_1^{*},u_2^{*})\) satisfies
\begin{equation}
  J_i(u_i^{*},u_j^{*})
  \le
  J_i(u_i,u_j^{*})
  \quad
  \text{for every feasible unilateral deviation }u_i.
\end{equation}

\subsection{Forward and inverse algorithms}

The forward game is solved by damped iterative best response:
\begin{enumerate}
  \item fix \(u_2\) and optimize \(u_1\);
  \item fix the updated \(u_1\) and optimize \(u_2\);
  \item damp the updates and repeat until the infinity-norm change in the
        joint control vector is below \(3\times10^{-3}\), subject to a
        20-iteration cap;
  \item verify that neither player obtains a material unilateral improvement.
\end{enumerate}
Each best response is an \texttt{fmincon} problem. The control-change test is
the primary stopping criterion. A separate post-solution test re-optimizes
each player's control while fixing its opponent and requires the largest
possible objective improvement to be below \(10^{-4}\). The implementation
can accept this second condition as a fallback convergence certificate when
the iteration cap is reached, but the demonstrations reported here do not
rely on that fallback: both meet the primary control-change tolerance after
16 of the 20 allowed iterations, with final gaps \(2.15\times10^{-3}\) and
\(2.49\times10^{-3}\). Their maximum unilateral improvements are
\(2.99\times10^{-7}\) and \(4.39\times10^{-7}\), respectively.

The inverse problem is formed separately for each player by differentiating
its four feature functionals with respect to its own control:
\begin{equation}
  \hat{\vect{w}}_i =
  \arg\min_{\vect{w}_i\ge0,\
             \vect{1}^{\mathsf T}\vect{w}_i=1}
  \|A_i\vect{w}_i\|_2^2
  +\lambda\|\vect{w}_i-\vect{w}_0\|_2^2.
\end{equation}

As at Level 3, the post-inference forward validation is deliberately
cold-started from the uniform configured guess \(u_1=u_2=0.35\) on every
control interval. The demonstrated controls are not supplied to the solver as
an initial guess. Consequently, the validation tests whether damped best
response can reconstruct a nearby Nash trajectory from a truth-independent
initialization, rather than merely taking one local sweep from the trajectory
being validated.

\noindent\begin{minipage}{\linewidth}
\begin{lstlisting}[style=pseudostyle,title={Compact MATLAB pseudocode}]
Input: model, initial states, candidate features for each player
for each of 2 synthetic experiments e
    demo_e <- solveOpenLoopNash(model, x0_e, truePlayerWeights)
    for each player i
        A_i,e <- finite-difference own features with respect to own control
        retain interior-control rows and stack into A_i
for each player i
    w_hat_i <- inferSimplexWeights(A_i, lambda)
for each experiment e
    validation_e <- solveOpenLoopNash(model, x0_e, w_hat,
                                      initial controls = 0.35)
verify control/state errors and unilateral-improvement gaps
Output: player-specific weights and Nash validation diagnostics
\end{lstlisting}
\end{minipage}

\subsection{Results}

\begin{table}[H]
\centering
\caption{Level 4 player-specific objective weights.}
\label{tab:level4}
\begin{tabular}{lrrrr}
\toprule
& \multicolumn{2}{c}{Player 1} & \multicolumn{2}{c}{Player 2}\\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
Objective kernel & True & Inferred & True & Inferred\\
\midrule
Own biomass & 0.5800 & 0.5771 & 0.6200 & 0.6191\\
Regularized effort & 0.2200 & 0.2177 & 0.2500 & 0.2502\\
Metabolite deficit & 0.0500 & 0.0586 & 0.0500 & 0.0504\\
Partner biomass & 0.1500 & 0.1465 & 0.0800 & 0.0803\\
\bottomrule
\end{tabular}
\end{table}

The Euclidean weight errors are \(1.00\times10^{-2}\) and
\(1.07\times10^{-3}\) for Players 1 and 2. Both cold-started validation
solves satisfy the primary control-change tolerance after 16 best-response
iterations and also pass the unilateral-deviation check
(Table~\ref{tab:level4-validation}).

\begin{table}[H]
\centering
\small
\caption{Level 4 cold-start forward validation. RMSE values compare the
recomputed Nash trajectories obtained with the inferred player weights
against the synthetic demonstrations.}
\label{tab:level4-validation}
\begin{tabular}{rrrrrr}
\toprule
Experiment & Iterations & Control RMSE & State RMSE &
Best-response gap & Max.\ improvement\\
\midrule
1 & 16 & \(1.564\times10^{-3}\) & \(2.509\times10^{-3}\) &
\(2.168\times10^{-3}\) & \(3.03\times10^{-7}\)\\
2 & 16 & \(1.165\times10^{-3}\) & \(2.220\times10^{-3}\) &
\(2.476\times10^{-3}\) & \(4.33\times10^{-7}\)\\
\bottomrule
\end{tabular}
\end{table}

The mean control and state RMS errors are \(1.365\times10^{-3}\) and
\(2.364\times10^{-3}\), respectively. These nonzero discrepancies are larger
than their warm-start counterparts but remain small on the allocation and
state scales of the case study.

The near-null-direction separation ratios are approximately 69.9 and 296 for
the two players. Player 1 remains less well-conditioned, consistent with its
larger weight error, particularly for the received-metabolite term.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/level4_idg.png}
\caption{Level 4 results for the first experiment. Dashed cold-start
forward-validation controls and trajectories, recomputed from the uniform
initial guess \(u_1=u_2=0.35\), closely track the equilibrium data. The lower
panels compare true and inferred objectives and show the two stationarity
spectra.}
\label{fig:level4}
\end{figure}

\section{Implementation map and verification}

\begin{table}[H]
\centering
\caption{Principal MATLAB components.}
\label{tab:software}
\begin{tabularx}{\textwidth}{>{\ttfamily}l X}
\toprule
\normalfont File & Purpose\\
\midrule
runInverseLadderCaseStudy.m & Top-level reproducible workflow.\\
defaultInverseLadderConfig.m & Parameters, experiments, objectives, and solver settings.\\
communityRhs.m & Five-state controlled microbial ODE.\\
simulateCommunity.m & Adaptive \texttt{ode15s} simulator.\\
simulateControlledModel.m & Fixed-step RK4 simulator used in optimization.\\
runLevel1ParameterEstimation.m & Bound-constrained multi-experiment calibration.\\
runLevel2ModelDiscovery.m & Integrated sparse growth-law discovery.\\
solveCommunityPlanner.m & Centralized forward optimal-control solver.\\
runLevel3InverseOptimalControl.m & Level 3 inverse stationarity and validation.\\
solveOpenLoopNash.m & Damped iterative best-response solver.\\
runLevel4InverseDifferentialGame.m & Player-wise inverse stationarity and Nash validation.\\
testInverseLadderCaseStudy.m & MATLAB unit tests.\\
\bottomrule
\end{tabularx}
\end{table}

The verified development environment is MATLAB R2026a Update 1 with
Optimization Toolbox, Global Optimization Toolbox, Statistics and Machine
Learning Toolbox, Signal Processing Toolbox, Symbolic Math Toolbox, Parallel
Computing Toolbox, and Control System Toolbox. The current implementation uses
the optimization, statistics, and signal-processing functionality directly.

\begin{table}[H]
\centering
\caption{Verification summary.}
\label{tab:verification}
\begin{tabular}{lr}
\toprule
Check & Result\\
\midrule
Complete workflow runtime without plotting & approximately 25 s\\
MATLAB unit tests & 4 of 4 passed\\
MATLAB Code Analyzer findings & 0\\
Saved numerical result file & verified\\
Saved summary figure & verified\\
Nash demonstration iterations & 16 of 20 (both experiments)\\
Nash cold-start validation iterations & 16 of 20 (both experiments)\\
Nash demonstration/validation gaps & \(\le 2.49\times10^{-3}\)\\
Nash unilateral-improvement check & \(<4.4\times10^{-7}\)\\
\bottomrule
\end{tabular}
\end{table}

\clearpage
\section[Interpretation and limitations]{\hspace{0.5em}Interpretation and limitations}

\subsection{What the case study demonstrates}

The case study provides a computationally coherent realization of the inverse
ladder:
\begin{itemize}
  \item Level 1 recovers continuous kinetic parameters when the structure and
        controls are known.
  \item Level 2 admits competing structural explanations even when prediction
        is accurate.
  \item Level 3 recovers an objective only after choosing candidate kernels,
        normalizing their scale, and assuming centralized optimality.
  \item Level 4 requires an equilibrium concept, player-specific actions, and
        separate stationarity conditions.
\end{itemize}

The increasing data and modeling requirements are visible in the code. Levels
1 and 2 use ordinary time-series observations and prescribed inputs. Levels 3
and 4 additionally require controls, a candidate objective basis, and a
forward optimal-control or game solver.

\subsection{Important limitations}

\begin{enumerate}
  \item \textbf{Synthetic ground truth.} The trajectories, objectives, and
        controls are generated by the same model class used for inference.
        This is appropriate for verification but does not establish biological
        validity.
  \item \textbf{Observed controls.} Real microbial studies rarely measure
        metabolic allocations directly. Transcript, protein, or secretion
        measurements would be required as proxies.
  \item \textbf{Objective basis dependence.} Objective recovery is conditional
        on the selected feature library. An omitted kernel can redistribute
        inferred weights without visibly degrading the fit.
  \item \textbf{Open-loop equilibrium.} Level 4 infers finite-horizon open-loop
        strategies. General nonlinear feedback Nash equilibria would require
        solving coupled Hamilton--Jacobi equations and are not attempted.
  \item \textbf{Two-player scale.} The best-response and finite-difference
        methods are suitable for a compact illustration, not yet for large
        microbial communities.
  \item \textbf{Local optimality information.} Inverse stationarity establishes
        compatibility with first-order conditions. Forward re-optimization and
        unilateral-deviation tests are therefore essential.
\end{enumerate}

\section{Recommended extensions}

The most informative next developments are:
\begin{enumerate}
  \item design additional asymmetric perturbations specifically to resolve
        the Level 2 allocation ambiguity;
  \item add measurement noise to the Level 3 and Level 4 controls and quantify
        confidence regions for the inferred weights;
  \item compare the centralized and game formulations on the same behavior to
        demonstrate cooperative mimicry explicitly;
  \item replace synthetic allocations with gene-expression or metabolite-flux
        measurements from an engineered two-strain consortium;
  \item add profile-based or Bayesian uncertainty quantification for objective
        weights;
  \item test whether inferred objectives predict behavior under dilution,
        nutrient, or partner-knockdown conditions excluded from inference.
\end{enumerate}

\section{Conclusions}

The MATLAB implementation shows that all four levels can be represented in a
single biological and computational framework. Parameter estimation and sparse
model discovery are inexpensive and mature enough for routine application.
Inverse optimal control is feasible for a small controlled community when
allocation trajectories are available. A two-player inverse differential game
is also computationally feasible under open-loop, finite-horizon assumptions,
but it introduces a substantially larger modeling commitment and more severe
conditioning issues.

The central lesson is not that the upper levels produce uniquely correct
biological objectives. Rather, the implementation demonstrates how to formulate
those hypotheses, diagnose their identifiability, and test their predictive
consequences. The Level 2 structural ambiguity and the Level 4 conditioning
difference between players are therefore substantive results: they make the
case study consistent with the manuscript's emphasis on both opportunities and
limitations.

\begin{thebibliography}{9}

\bibitem{BangaVillaverde2026}
J. R. Banga and A. F. Villaverde,
\emph{A Hierarchical Framework for Inverse Problems in Biological
Cybernetics: Opportunities and Limitations},
manuscript, 2026.

\bibitem{BruntonKutz2022}
S. L. Brunton and J. N. Kutz,
\emph{Data-Driven Science and Engineering: Machine Learning, Dynamical
Systems, and Control},
Cambridge University Press, 2022.

\bibitem{MessengerBortz2021}
D. A. Messenger and D. M. Bortz,
``Weak SINDy: Galerkin-based data-driven model selection,''
\emph{Multiscale Modeling \& Simulation}, vol. 19, no. 3,
pp. 1474--1497, 2021.
doi: 10.1137/20M1343166.

\bibitem{Tsiantis2018}
N. Tsiantis, E. Balsa-Canto, and J. R. Banga,
``Optimality and identification of dynamic models in systems biology:
an inverse optimal control framework,''
\emph{Bioinformatics}, vol. 34, no. 14, pp. 2433--2440, 2018.
doi: 10.1093/bioinformatics/bty139.

\bibitem{Molloy2022}
T. L. Molloy, J. Inga, S. Hohmann, and T. Perez,
\emph{Inverse Optimal Control and Inverse Noncooperative Dynamic Game Theory},
Springer, 2022.

\bibitem{Zomorrodi2012}
A. R. Zomorrodi and C. D. Maranas,
``OptCom: A multi-level optimization framework for the metabolic modeling and
analysis of microbial communities,''
\emph{PLoS Computational Biology}, vol. 8, no. 2, e1002363, 2012.

\end{thebibliography}

\end{document}
